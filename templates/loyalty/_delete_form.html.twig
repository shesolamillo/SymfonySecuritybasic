<form method="post" action="{{ path('app_loyalty_delete', {'id': loyalty.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ loyalty.id) }}">
     <button class="btn btn-danger">Delete</button>
</form>
