<form method="post" action="{{ path('app_activity_log_delete', {'id': activity_log.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activity_log.id) }}">
    <button class="btn">Delete</button>
</form>
