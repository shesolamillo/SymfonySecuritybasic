{% extends 'base.html.twig' %}

{% block title %}Edit Customer{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/customer_edit.css') }}">
{% endblock %}

{% block body %}
<div class="container">
    <div class="card">
        <h1>Edit Customer</h1>

        <div class="form-wrapper">
            {{ include('customer/_form.html.twig', {'button_label': 'Update Customer'}) }}
        </div>

        <div class="actions">
            <a href="{{ path('app_customer_index') }}" class="back-link">← Back to List</a>
            {{ include('customer/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
